var person = {
  name: 'Olivia'
};

person.age = 23;

debugger; // This lets you run through the program until this line with the command 'C' in terminal

person.name = 'Chrissy';

console.log(person);
